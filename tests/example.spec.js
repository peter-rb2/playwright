// @ts-check
const { test, expect } = require("@playwright/test");

test.describe("Login", () => {
  test("Validate username and password", async ({ page }) => {
    // ...
  });

  test("Validate if user is a registered or not,", async ({ page }) => {
    // ...
  });

  test("Test that all fields are mandatory", async ({ page }) => {
    // ...
  });
});
